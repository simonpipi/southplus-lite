# 项目协作规则

- 每次回答使用中文。
- 对范围小、目标明确的需求，例如单点 Bug、少量文件改动、文案或配置调整，默认直接定位、修改并验证。
- 小需求不要启用 superpower 系列技能，不创建设计文档、实施计划或 worktree，不派发子代理，也不要求多轮方案确认，除非用户明确要求。
- 只有跨模块、需求不明确、高风险，或用户明确要求正式方案时，才使用设计和计划流程。
- 回复保持简洁，优先说明修改结果和验证结果。
