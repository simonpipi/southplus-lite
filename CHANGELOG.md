# Changelog

## 0.5.3 - 2026-08-18

### Added

- 新增保守自动领取任务：仅在打开社区论坛任务页时检查日常 / 周常可领取入口，发现可领取链接后按顺序请求领取，并同步已完成任务记录。
- 设置面板新增 `自动领取任务` 开关，默认开启；已完成任务页只同步领取记录，不执行领取动作。

## 0.5.2 - 2026-08-17

### Added

- 新增已完成任务页下方的内联任务领取记录：可同步日常 / 周常领取记录，在本地保留最近 100 次，并支持搜索、任务类型筛选和删除单条本地记录。
- 我的内容中心新增任务领取概览和已完成任务页跳转；备份、数据健康和本地体积统计同步纳入任务领取记录。

## 0.5.1 - 2026-08-17

### Fixed

- 修复阅读页摘要卡 / 阅读记录在同一页滚轮上翻时回退百分比的问题；现在同一页保留最大阅读进度，进入后续分页仍会更新记录。
- 修复图墙模式卡片底部作者、日期等信息受原站浅色字体影响，在浅色卡片背景上不可读的问题。

## 0.5.0 - 2026-08-16

### Added

- 阅读页新增帖子详情摘要卡，集中展示收藏 / 稍后、阅读进度、未读 / 上次楼层、资源数量、图片数量和自动购买状态，并提供继续阅读、稍后、收藏和资源工作台入口。
- 悬停预览弹层新增状态条，直接展示当前主题的收藏状态、稍后状态和已识别 / 标题推测的资源类型角标。
- 新增自定义批量确认层：删除站内收藏、清空 / 移除稍后看、清空 / 移除阅读记录、删除资源、清空资源和批量标记资源前展示影响数量和对象摘要。
- 工具栏新增“我的内容”中心，汇总稍后看、最近浏览、资源工作台、自动购买记录和本地收藏标记，并提供快捷跳转。
- 设置面板新增轻量 / 资源 / 阅读三个配置预设，只批量切换脚本设置，不覆盖收藏、稍后看、资源库或阅读进度。

### Changed

- 导航中心标题显示入口数量，并在首次使用时默认收起低频导航分组；用户手动展开后不会被后续刷新重新收起。

### Fixed

- 修复阅读页帖子详情摘要卡按视口宽度撑开，导致右侧指标卡被内容列裁切的问题；摘要卡现在跟随当前内容列宽度，指标卡会自动换行。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`
- `node tests/browser-smoke.js`（真实站点 sanity check：48 个断言通过，阅读页固定资源栏场景因实页无资源仅提示警告）
- 本地 HTTP 夹具 + headless Chrome 加载当前 `southplus_enhancer.user.js`，确认帖子详情摘要卡、配置预设、我的内容中心和阅读页资源栏均生成。
- 本地 HTTP 夹具 + headless Chrome 窄内容列验证：摘要卡宽度等于内容列，指标卡自动换行且不产生横向溢出。

## 0.4.12 - 2026-08-15

### Fixed

- 将原站图墙入口同步恢复到新版论坛列表标题栏右侧，避免只放入左侧导航时因导航分组折叠或本地缓存导致用户看不到。
- 将 `thread_new.php` 图墙页识别为论坛列表页，点入图墙模式后继续套用列表页增强逻辑。
- 图墙页按当前清爽卡片样式重排，覆盖原站绝对定位瀑布流，桌面和移动端都使用响应式卡片网格。
- 图墙卡片补齐列表页已有的本地 `稍后` 和站内 `收藏` 操作，并保留顶部过滤、可见稍后和预载下页能力。
- 修复图墙最后一排卡片在原站图墙脚本二次布局后没有补上 `稍后` / `收藏` 的问题，按钮统一固定到作者信息下方。
- 修复图墙卡片标题、回复/人气和资源角标互相覆盖的问题，图墙卡片不再把资源角标插入标题区域，并清理原站浮动布局。
- 图墙页标题栏和左侧导航新增 `列表模式`，可从 `thread_new.php` 切回同版块普通列表。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.11 - 2026-08-15

### Fixed

- 恢复论坛列表页左侧导航里的原站图墙入口：仅当当前版块原始页面实际提供 `thread_new.php` 的图墙模式链接时显示，不再把图墙模式写入跨页面持久导航池，也不采集皮肤切换链接。

### Verified

- 原站抽样确认：茶馆、询问&求物、同人音声、AI交流等公开版块存在 `[点击进入图墙模式]`，部分版块不存在该入口。
- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.10 - 2026-08-13

### Added

- 预览图侧栏新增 `下载全部`：可下载当前筛选范围内的预览图并打包为 ZIP，侧栏内显示总进度、折叠下载详情和逐图状态。
- 预览图下载失败会自动重试最多 6 次；达到上限后可继续重试失败项，也可跳过失败图片并将已完成图片打包下载，ZIP 内附 `download-report.txt` 失败清单。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.9 - 2026-08-13

### Changed

- 预览图集、悬停预览弹层和灯箱缩略图在图片加载完成前保持占位，加载完成后再淡入，避免渐进解码阶段的临时花屏直接显示。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.8 - 2026-08-12

### Changed

- 悬停预览增加意图延迟：鼠标快速扫过主题列表时不再立刻请求帖子详情，网络友好模式下保持更保守的触发间隔。
- 鼠标移出主题后会取消尚未完成的悬停预览请求，避免离开后继续解析无用响应。
- 悬停预览弹层图片改为先写入懒加载地址，弹层停留后再按小批次加载，减少误触预览带来的图片请求。
- 缓存命中的悬停预览增加极短延迟并继续复用本地缓存，降低列表快速移动时的弹层闪烁。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.7 - 2026-08-12

### Changed

- 预览图集改为延迟扫描正文图片：阅读页初始只挂载收起入口，首次展开或浏览器空闲时再收集候选图片并渲染缩略图。
- 预览图集和灯箱缩略图改为容器事件委托，减少图片较多时为每张缩略图绑定的点击 / 悬停处理。
- 阅读页固定资源栏按钮改为统一事件委托，资源较多或频繁筛选重绘时减少重复事件绑定。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.6 - 2026-08-12

### Changed

- 首屏启动改为按需创建工具箱、设置面板和命令面板，减少页面刷新后的同步初始化工作。
- 总增强入口按页面类型分流执行，首页、列表页、阅读页、个人中心和任务页只运行对应增强模块。
- 命令面板缓存当前页面命令索引，并对搜索输入增加轻量防抖，降低连续输入时的重绘和 DOM 扫描频率。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.5 - 2026-08-12

### Fixed

- 修复阅读页预览图侧栏图片过多时，滚动到底部后头部操作区不可见、无法直接收起预览框的问题。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.4 - 2026-08-11

### Added

- 新增阅读页固定资源栏：右侧汇总当前帖子里的网盘、磁力、种子、压缩包、电驴和外链资源，显示类型、楼层、作者、口令和保存 / 处理状态。
- 资源栏支持按资源类型筛选、复制全部、复制口令、保存当前、单条复制、定位楼层，以及标记待下载 / 已处理 / 失效。
- 移动端资源栏改为底部抽屉布局，收起后保留 `资源 N` 悬浮入口。

### Changed

- 阅读页资源面板、自动购买保存、资源工作台状态变更后会同步刷新固定资源栏，避免资源状态显示滞后。
- 快速回复编辑器识别会排除固定资源栏，避免把资源栏内控件误当成回复输入区。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.3 - 2026-08-11

### Added

- 新增论坛列表资源类型角标：标题旁显示百度、夸克、PikPak、磁力、种子、压缩包、电驴、外链和网盘等标签。
- 角标来源区分真实识别和标题推测：资源库 / 悬停预览识别使用实线角标，标题关键词推测使用虚线角标。
- 论坛工具条新增资源类型筛选，点击工具条筛选项或行内角标可只看同类资源主题。
- 新增 `resource-badge-preview.html` 静态交互预览页，用于确认列表角标、筛选和推测样式。

### Changed

- 列表快速过滤纳入资源角标文本，搜索 `夸克`、`磁力` 等资源类型时可命中对应主题。
- 悬停预览拿到帖子 HTML 后会顺手刷新当前行资源角标，不额外批量请求帖子详情。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.4.2 - 2026-08-10

### Added

- 新增全局命令面板，可通过右下角 `命令` 或 `Ctrl+K` 打开，统一搜索导航、当前页主题、稍后看、最近浏览、资源工作台、设置项和页面动作。
- 命令结果支持分类筛选、上下键选择、Enter 执行；有 URL 的结果可跳转，中心 / 工具 / 设置类结果打开对应面板。
- 新增 `command-palette-preview.html` 静态交互预览页，保留与现有清爽产品型主题一致的布局、颜色、弹层和移动端单列预览。

### Fixed

- 修复全局命令面板搜索框会在中文输入法组词期间重绘，导致中文无法正常输入的问题。
- 修复命令详情中的长链接目标在右侧详情卡片里显示拥挤或截断的问题。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`
- 浏览器验证全局命令面板渲染、`Ctrl+K` 打开、资源搜索、分类切换和桌面 / 移动宽度无横向溢出。

## 0.4.1 - 2026-08-10

### Added

- 快捷回复升级为右下角浮动回复框，支持最小化 / 继续回复、常用回复填入、楼层回复上下文、选中文字引用和 Ctrl+Enter 发送。
- 浮动回复框直接加载原站 `smallface` 表情图片，点击插入对应 `[s:n]` 表情码。
- 图片发送复用原站回复表单的附件和描述字段，选择图片后在浮窗内展示缩略图、文件名和大小。

### Changed

- 常用回复按钮不再点选即提交，改为先填入编辑器，由用户确认后发送，降低误发概率。

### Fixed

- 修复楼层回复按钮可能触发表单提交导致页面刷新的问题。
- 修复浮动回复框初始即显示的问题，改为点击回复入口后再展开。
- 修复最小化后的“继续回复”按钮遮挡右下角工具栏的问题。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`
- Chrome 夹具验证浮动回复框渲染、原站表情加载、楼层回复、图片附件预览、最小化 / 继续回复和桌面 / 移动宽度无横向溢出。

## 0.4.0 - 2026-08-09

### Added

- 首页新增 `论坛仪表盘`，在首页模块前汇总今日浏览、收藏新增、未读收藏、资源新增、稍后看积压和网络友好请求状态。
- `值得回看的主题` 改为真实本地评分：综合站内收藏、稍后看、阅读进度、资源工作台、今日新增、标签和已读完成状态排序，优先提示已表达兴趣但未处理完的主题。
- 仪表盘新增最近动态、资源速览、高频标签和本地积压模块，并支持刷新摘要、打开稍后看和复制日报。

### Changed

- 设置面板和工具箱新增 `论坛仪表盘` 开关，默认开启；仍复用首页模块和左侧导航中心，不新增后台远程扫描。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`
- Chrome 实页验证首页仪表盘渲染、导航中心入口、桌面 / 移动宽度无横向溢出。

## 0.3.9 - 2026-08-07

### Changed

- `我的收藏` 徽标新增站内收藏数量短期缓存，页面刷新时优先显示缓存数量，缓存过期后才后台刷新收藏页，降低连续刷新时的额外请求。
- 网络友好模式将 Cloudflare 520 错误纳入脚本请求冷却识别，遇到源站短暂异常时暂停后台增强请求并保留旧缓存数量。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.3.8 - 2026-08-07

### Added

- `我的收藏` 展开面板新增站内收藏删除能力，支持单条删除和勾选后批量删除站内收藏，删除请求会同步到原站收藏夹并刷新面板统计。

### Fixed

- 修复站内收藏删除请求误用原站默认 `job=change`，导致只从面板临时移除、刷新后收藏又出现的问题；现在删除会提交原站 `job=clear`。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.3.5-0.3.7 - 2026-08-07

### Fixed

- 修复帖子详情页在清爽 / 沉浸阅读模式下隐藏顶部个人信息区，导致头像、等级、帖子数和“我的主题 / 我的回复”缺失的问题。
- 修复搜索页正文输入框和左侧 `导航中心` 搜索框共用样式，导致导航搜索框变宽、字号变大并撑出页面安全范围的问题。
- 修复搜索页左侧 `导航中心` 的“搜索”导航项和顶部导航过滤输入框共用 `spx-module-nav-search` 类名，导致输入框 / 导航项样式互相污染的问题。
- 搜索页正文表格增加 `width:100%`、`min-width:0` 和固定布局约束，避免原站表格或输入框继续撑出右侧内容列。
- 修复搜索页真实 DOM 中搜索表格被包在 `display:inline` 的 `form` 内，导致 `.t` 卡片按外层 1480px 宽度计算并撑出右侧正文列的问题。
- 搜索页正文 `form`、`form > .t` 和表格统一收束到导航中心右侧内容列，移除异常右侧负边距，避免页面出现横向滚动。

### Verified

- 当前已打开的 Chrome 搜索页现场验证：临时注入同等 CSS 后，`.spx-module-body` / `form` / `.t` 宽度均为 1206px，`documentElement.scrollWidth` 从 1854px 回到 1680px。
- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.3.4 - 2026-08-07

### Fixed

- 修复主导航 `我的收藏` 徽标刷新后只显示站内收藏数量，和弹层里的完整“我的收藏”口径不一致的问题；现在徽标显示 `站内收藏 + 稍后看` 总数，加载前显示 `...`，读取失败显示 `-`。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.3.3 - 2026-08-06

### Added

- 帖子列表行内操作新增站内 `收藏`，点击后直接调用 South Plus 原生收藏接口保存当前主题。
- 悬停预览弹层新增 `收藏` 按钮，可在预览里直接把帖子加入站点收藏夹。

### Changed

- 帖子列表行内工具将原 `本页屏人` 替换为更高频的站内收藏入口，保留 `屏人` 和 `隐藏` 用于屏蔽 / 临时隐藏。

### Fixed

- 修复列表和悬停预览里点击 `收藏` 时缺少站点 `verifyhash` / `nowtime`，以及纯数字主题 ID 未被识别，导致原站返回“非法请求”或直接显示收藏失败的问题。
- 修复站内收藏成功后主导航 `我的收藏` 徽标和已打开收藏面板不即时同步，需要刷新页面才更新的问题。
- 修复阅读页使用原站自带 `收藏` 操作后，主导航 `我的收藏` 徽标没有跟随成功提示即时更新的问题。
- 修复刷新页面后主导航 `我的收藏` 徽标误用本地稍后看数量，导致和站内收藏数量不一致的问题；现在会自动读取站内收藏页，加载前显示 `...`。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.3.2 - 2026-08-05

### Added

- 主导航右侧新增 `我的收藏` 高频入口，可直接打开站内收藏页。
- `我的收藏` 展开面板支持站内收藏 / 本地稍后看切换、关键词搜索、最近 / 未读 / 资源 / 图片 / AI 快捷筛选、排序、复制链接和分批加载，收藏很多时不会一次性渲染全部记录。

### Changed

- 站内收藏和本地稍后看继续保持独立数据边界，主导航弹层只做快速查看和跳转，不把两类记录混写。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.3.1 - 2026-08-05

### Added

- 悬停预览弹层新增 `打开帖子`、`加入稍后` 和 `复制链接` 操作，可在预览中直接完成常用列表动作。
- 悬停预览现在会显示 `已缓存`、`加载中`、`加载失败` 和 `请求冷却中` 等状态，失败结果会短暂缓存，避免鼠标反复划过同一主题时重复请求。

### Changed

- 悬停预览图片最多展示前 6 张，并保留“打开帖子查看其余图片”的提示，减少预览弹层额外图片加载。
- 将悬停预览状态文案、图片限制和动作区拆成小 helper，并补充对应单元断言，降低后续继续扩展预览功能的维护成本。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.3.0 - 2026-08-05

### Added

- 新增默认开启的 `网络友好模式`，脚本主动发起的后台增强请求会进入统一队列，避免导航刷新、悬停预览、预载下页、快捷回复刷新和自动购买刷新在同一秒内集中请求。
- 新增 504 / HTTP 429 / HTTP 503 和“操作频繁”页面识别，命中后进入短暂冷却，后台预览请求会临时暂停并给出提示。

### Changed

- 导航中心后台发现从并发预抓取改为低优先级串行刷新，并为同一组发现 URL 增加 6 小时刷新间隔。
- 悬停预览新增近期结果缓存和离开后过期渲染保护，减少鼠标快速扫过列表时的重复请求。
- 快捷回复优先复用提交响应里的帖子 HTML，只有响应不能替换当前帖子时才延迟刷新帖子页。
- 自动购买余额检查增加短时间缓存，购买和购买后刷新走高优先级队列，购买后会清空余额缓存，避免使用旧余额。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.12 - 2026-08-05

### Added

- 导航中心支持搜索导航项、星标置顶常用入口，并将置顶记录纳入本地备份和体积统计。

### Changed

- 导航中心新增紧凑 / 标准 / 舒适三档密度设置，舒适档默认保留 260px 左栏、更高纵向可视高度和加高条目块区域。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.11 - 2026-08-04

### Fixed

- 修复多页帖子或回复页提交后仍刷新原地址，导致新回复没有在当前页面无刷新展示的问题；现在会优先跟随服务端重定向地址，缺失时从表单 `tid` 回到帖子末页加载最新楼层。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.10 - 2026-08-03

### Added

- 导航中心一级分组标题改为可点击折叠 / 展开，折叠状态保存到本地，刷新或跨页面进入时会沿用上次状态。

### Fixed

- 修复首页版块模块沿用旧“版主”列导致表头换行、右侧 `主题/文章` / `最后发表` 过度贴边的问题，版块表现在收敛为更紧凑的三列布局。
- 清理首页版块行 `th` 单元格继承的旧站底边框，去掉子版链接下方多余横线。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.9 - 2026-08-03

### Fixed

- 修复个人中心页签只有 `我的主题` / `我的回复` 接入左侧 `导航中心` 的问题，个人首页、资料、主题、回复、收藏、好友、商品、消息和设置页现在都会保留导航中心和对应高亮。
- 修复阅读页启用左侧 `导航中心` 后面包屑、帖子表格、楼层工具条和正文主列仍按旧的 1480px / 1680px 宽度渲染，导致右侧内容撑出屏幕的问题。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`
- Chrome 实页采样：阅读页左侧导航模式下面包屑、帖子表格、楼层工具条和正文右侧均已限制在内容列内。

## 0.2.8 - 2026-08-03

### Added

- 新增夜间模式开关，可通过工具箱或设置面板切换深色影院式主题，覆盖阅读页、列表、弹层、图集、灯箱和资源工作台。
- 首页、论坛列表页、阅读页、最新帖子、搜索结果、我的主题和我的回复页统一接入左侧常驻导航中心，支持站点入口、首页模块、论坛子栏目、当前位置、上一主题 / 下一主题等真实导航入口，移动端自动降级为横向滚动入口。

### Changed

- 默认视觉收敛为清爽产品型主题，统一核心颜色、面板、边框、阴影、导航 active 状态和操作强调色的语义变量。
- 首页顶部快捷卡片停止生成，相关站点入口统一收纳到左侧导航中心，避免同一导航在页面上方和左侧重复出现。
- `导航中心` 改为全页面共享导航池：固定保留站点入口，跨页面保留已发现的首页模块、论坛子栏目和阅读页版块路径，并纳入本地备份、导入和本地体积统计。
- `导航中心` 改为父子层级展示：站点入口保持独立分组，首页模块和论坛子栏目会按真实父级收纳，避免跨页面导航池里的菜单全部同级堆叠。
- 左侧导航宽度从 `152px` 扩到 `220px`，桌面布局左栏最小宽度同步提高到 `196px`；全站布局继续按清爽产品型预览收敛为左侧导航 + 右侧产品面板。
- 论坛列表收起面包屑、子版块和顶部分页等旧站前置块，改为产品化版块标题和主题表格；行内操作默认隐藏，悬停时再显示。
- 论坛页主导航隐藏旧登录浮层并加入 `South Plus +++` 品牌，避免顶部文字挤叠。

### Fixed

- 修正清爽产品型落地样式与静态预览差距：顶部导航改为 38px 产品导航，页面宽度回到 1480px，首页 / 论坛卡片、表头、模块导航和右下工具条更贴近预览稿。
- 修复论坛列表在缺少 `#content` 时右侧内容撑回全宽、左侧模块导航把右侧后续内容向下顶出大空档、夜间首页模块白底残留等布局问题。
- 修复左侧栏混入页面操作、阅读开关、我的中心、脚本设置、筛选入口和右侧状态徽标的问题，左侧 `导航中心` 保持纯导航列表。
- 修复阅读页导航缺少当前位置、当前版块 / 子版块、上一主题 / 下一主题以及跨页面导航池冷启动补齐不足的问题。
- 修复导航中心跨页面数据重复、同父级重复、同名二级项重复、全局入口重复和 `图墙模式` 视图切换链接误采集的问题。
- 修复论坛列表快速过滤、`隐藏`、`本页屏人` 和本地屏蔽规则被列表行 `display:grid!important` 覆盖而无法隐藏的问题，所有列表隐藏状态统一同步到行级 inline display。
- 修复 `最新帖子`、`我的主题`、`我的回复` 和搜索结果页启用左侧 `导航中心` 后遮挡或撑出列表的问题，这些页面保留导航中心并把正文卡片限制在右侧安全列。
- 修复首页接入左侧 `导航中心` 后部分模块仍按半宽栅格展示，导致模块卡片和下方列表不对齐的问题。
- 修复社区论坛任务页没有接入左侧 `导航中心`，且右侧任务列表数据被全局卡片宽度撑出屏幕的问题；任务页现在兼容原站内层包裹结构，把功能简介 / 操作 / 版权归入正文侧栏，任务列表数据保留在正文主列。
- 修复点击热门板块或子版块导航后左侧 `导航中心` active 项不对应的问题，导航高亮改为按精确链接和 forum `fid` 匹配。
- 修复夜间模式下列表悬停预览弹层标题、元信息和正文仍使用浅色主题深色字体，导致文字难以辨认的问题。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`
- Chrome 实页采样：首页清爽 / 夜间主题、论坛列表桌面两列模块导航、阅读页左侧导航、导航池冷启动和 `图墙模式` 过滤均已验证。

## 0.2.7 - 2026-07-31

### Fixed

- 修复论坛页“子版块”目录表格被帖子列表网格样式误命中，导致版块行在大屏下横向拉散的问题。
- 收回 2K 以上屏幕的页面最大宽度，避免导航、过滤栏和论坛表格过度铺满屏幕。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.6 - 2026-07-30

### Changed

- 重构注入样式的基础变量，统一页面宽度、阅读行长、弹层宽度、圆角、阴影、主色和危险色。
- 页面容器、列表页、阅读页和沉浸阅读改为随屏幕自适应；正文按舒适行长控制，图片、列表和资源区域继续按可用空间展开。
- 设置面板按阅读体验、列表增强、自动购买、本地屏蔽、快捷回复、备份恢复和数据管理分组，并将清空类危险操作独立弱化。
- 工具箱降低头部信息密度，并按列表页 / 阅读页上下文调整功能排序。
- 稍后看、最近浏览、自动购买记录和资源工作台统一中心面板视觉层级，资源工作台加宽并区分主操作、次操作和危险操作。
- 预览图侧栏按视口自适应，移动端工具栏与弹层改为底部横栏 / 抽屉布局，并兼容 `safe-area-inset-bottom`。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.5 - 2026-07-30

### Changed

- 帖子真实正文容器 `#read_tpc.f14` 统一调整为 `16px`，行高收为约 `1.78`，兼顾可读性和紧凑度。
- 沉浸阅读模式忽略本地旧 `immersiveFontSize` 设置，避免旧值继续把正文顶回 18px 或 20px。

### Fixed

- 修复只调整外层 `.tpc_content` 导致内层 `#read_tpc.f14` 仍保留站点 `14px` 字号的问题。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.4 - 2026-07-30

### Changed

- 帖子正文默认字号调大到适中尺寸，并收紧正文换行行距，改善“帖子间距大但字偏小”的阅读观感。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.3 - 2026-07-30

### Fixed

- 工具箱弹出层与设置面板改为互斥显示，避免同时打开时重叠遮挡。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.2 - 2026-07-30

### Added

- 预览图面板新增侧栏模式和瀑布流布局，可在正文内展示或固定到右侧并一键收起。
- 预览图复制能力新增按楼层分组复制和 Markdown 图片清单复制，缩略图标签同步展示楼层和作者。
- 预览图灯箱新增底部缩略图导航，支持快速跳转到指定图片。

### Changed

- 预览图面板进入帖子时默认以右侧收起状态展示，避免首次打开帖子占用正文阅读区域。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.1 - 2026-07-30

### Added

- 设置面板新增独立 `本地体积` 入口，以面板形式展示设置、已读、稍后看、阅读进度、自动购买记录和资源库的 localStorage 占用、记录数量、上限比例和清理建议。

### Fixed

- 修复回复页快捷回复可能挂到隐藏设置面板输入框里，导致当前页面回复区看不到快捷回复按钮的问题。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.2.0 - 2026-07-30

### Added

- 资源中心升级为资源工作台：支持资源勾选、多选批量复制、批量标记待下载 / 已处理 / 失效，以及选中资源备注和标签维护。
- 新增来源帖分组视图，可按帖子聚合资源，并支持选择分组、复制分组和复制 Markdown 清单。
- 资源记录新增备注和标签展示，搜索与筛选可覆盖备注 / 标签，下载清单和 Markdown 清单同步输出备注、标签和状态。

### Changed

- 资源工作台顶部统计会显示待下载、已选数量和来源分组状态，便于大批资源整理。

### Fixed

- 修复资源工作台筛选栏在控件较多时搜索框被挤成窄空白框的问题，控制项会自动换行并保留可读宽度。
- 修复资源工作台等中心面板搜索框中文输入法组词被重渲染打断的问题，组词结束后再触发筛选。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.12 - 2026-07-29

### Changed

- 预览图面板改为首批渲染 36 张图片，滚动到底部或点击 `加载更多图片` 再追加下一批，减少多图帖子首次打开时的 DOM 和图片加载压力。
- 预览图缩略图启用浏览器原生懒加载，悬停放大图改为鼠标悬停或键盘聚焦时再加载，避免重复提前请求大图。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.11 - 2026-07-29

### Added

- 数据健康面板新增本地存储体积统计：按设置、已读、稍后看、阅读进度、自动购买记录和资源库展示 JSON 文本估算大小、记录数量和上限提示。

### Changed

- 数据健康统计补齐资源库输入，清理建议会提示接近记录上限或单项占用较高的数据类别，方便先导出备份再清理。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.10 - 2026-07-29

### Changed

- 设置面板移除重复的 `隐藏广告` 和 `首页模块全屏` 开关；对应功能仍保留在工具箱内，并继续按原设置生效。
- 自动购买开关和购买价格上限恢复为全局设置入口，不再只在阅读页显示，避免从列表页或首页打开设置时看不到 SP 限制。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.9 - 2026-07-29

### Added

- 新增分组工具箱：右下角工具栏保留 `顶部`、`底部`、`工具`、`设置` 四个高频入口，其他功能在工具箱内按页面导航、阅读模式、我的中心和设置分组展示完整名称与说明。
- 扩展常用 Plus 镜像域名匹配，覆盖 South / North / Level / Soul / Snow / Spring / Summer / Blue / White Plus 的根域和子域访问方式。

### Changed

- 优化工具栏与工具箱视觉样式：按钮改为更清晰的两字入口，工具箱增加分组数量、卡片悬停态、激活态和移动端单列布局。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.8 - 2026-07-28

### Changed

- 回复表单原生 `提交` 按钮接入无刷新提交：点击页面自带提交按钮后，会复用 AJAX 提交并局部刷新帖子内容，避免整页刷新。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.7 - 2026-07-28

### Added

- 资源中心新增待下载队列处理：支持复制当前筛选范围内的待下载资源，并可将当前筛选资源导出为带类型、提取码、来源帖和状态的文本清单。

### Changed

- 增强资源识别：新增电驴 `ed2k`、压缩包 / 镜像文件、更多跳转包装参数和提取码 / 访问码 / 解压码文案识别，自动购买资源跳转与资源中心同步支持这些类型。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.6 - 2026-07-27

### Changed

- 优化单文件结构：拆分样式规则生成和工具栏配置构建，降低后续维护导航、阅读布局、中心面板和工具栏入口时的改动范围。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.5 - 2026-07-26

### Added

- 新增资源中心：工具栏 `源` 入口可统一查看已保存的磁力、种子、百度网盘、其他云盘和外链资源，支持搜索、按状态 / 类型筛选、复制筛选结果、标记待下载 / 已处理 / 失效、来源帖跳转和批量清理。

### Changed

- 资源面板新增 `保存当前`，自动购买成功识别到资源后也会自动存入资源库；本地备份、导入预览和数据健康检查同步纳入资源库记录。
- 资源库记录按资源类型 + 规范化 URL 去重合并，兼容旧 key 或外链包装解析后的地址，避免同一网盘 / 种子重复保存。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.4 - 2026-07-26

### Added

- 新增购买后资源跳转提示：自动购买成功并刷新帖子内容后，会识别磁力、种子、百度网盘、其他云盘和外链，提供快捷打开、复制全部和资源面板入口。

### Changed

- 增强资源链接识别：支持还原论坛外链跳转包装、`.torrent` 种子地址、网盘提取码，以及夸克、阿里、UC、115、蓝奏、天翼、123 云盘、微云、PikPak、迅雷云盘等多类云盘域名。
- 调整顶部导航为低饱和深灰配色，统一左侧入口与主导航文字颜色，小幅放大导航字号，并强化当前选中项对比。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.3 - 2026-07-23

### Added

- 新增本地数据健康检查：设置面板可查看设置、已读、稍后看、阅读进度和自动购买记录统计，并清理重复 / 过期 / 异常数据。
- 新增资源链接提取器：阅读页楼层工具提供 `资源` 入口，可提取磁力、网盘、图片和外链，支持按全部楼层、本楼、作者或类型筛选复制。

### Changed

- 备份导入改为先读取并展示备份数据预览，再确认是否覆盖当前本地数据。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.2 - 2026-07-20

### Added

- 新增预览图批量复制：阅读页预览图面板可一键复制当前显示的全部原图链接。
- 新增预览图大图筛选：预览图面板支持 `只看大图`，灯箱切图和批量复制会跟随当前筛选结果。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.1 - 2026-07-20

### Added

- 新增中心面板搜索和筛选：`存`、`历`、`买` 三个中心支持按标题、楼层、状态或记录内容快速过滤。
- 新增中心面板批量处理：稍后看和最近浏览支持移除当前筛选结果，自动购买记录支持删除当前筛选结果。
- 新增楼层级阅读进度：阅读进度记录会保存当前楼层和下一未读楼层，支持从中心或阅读页首楼工具跳转 `未读楼层` / `上次楼层`。
- 新增稍后看 / 最近浏览标签分组：可为记录编辑标签，并在中心面板按标签筛选。
- 新增本地备份导出 / 导入：设置面板可导出设置、已读、稍后看、阅读进度和自动购买记录，并支持 JSON 备份覆盖导入。

### Changed

- `继续阅读` 从单纯恢复滚动位置升级为优先跳到下一未读楼层，保留 `上次楼层` 入口用于回到最后读到的楼层。
- 阅读进度更新会保留已有标签，避免滚动记录刷新时丢失手动分组。

### Fixed

- 修复楼层级续读误取每层重复 `topic` 锚点的问题，现在优先使用唯一的 `td_*` / `read_*` 楼层锚点。
- 修复阅读页跳转按钮使用旧进度状态的问题，现在点击 `未读楼层` / `上次楼层` 前会读取最新阅读进度。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`

## 0.1.0 - 2026-07-15

### Added

- 新增稍后看中心：支持查看、继续阅读、打开、移除、清空已保存主题。
- 新增阅读进度记录：保存页码、滚动位置和阅读百分比，支持一键继续阅读。
- 新增最近浏览中心：按最近阅读时间展示历史记录，支持继续阅读、打开、移除、清空。
- 新增自动购买记录中心：展示检查、跳过、购买中、完成、失败记录，支持打开帖子、删除单条记录、清空全部记录。
- 新增列表页批量操作：`可见稍后` 可保存当前筛选可见主题，`预载下页` 可预取下一页列表。
- 新增预览图灯箱：支持键盘切图、缩放和复制原图地址。
- 新增自动购买执行记录，避免刷新后重复触发同一帖子 / 楼层的购买流程。

### Changed

- 阅读进度达到显示口径的 `100%` 后不会被后续较低进度覆盖。
- 稍后看中心打开时会实时刷新阅读进度，不需要重新打开面板。
- 内部结构重构：抽象稍后看 / 最近浏览 / 自动购买记录三套中心面板，工具栏入口改为配置化生成，自动购买流程拆分为独立辅助函数，便于后续继续扩展。
- README 增加安装、更新、发布前验证和本地数据说明。

### Verified

- `node --check southplus_enhancer.user.js`
- `node southplus_enhancer.test.js`
- `git diff --check`
- 在普通 Google Chrome 的 South Plus 页面中临时注入验证：列表页批量稍后看、预载下页、最近浏览中心、自动购买记录中心。

## 0.0.3 - 2026-07-10

### Added

- 基础页面布局优化、阅读排版优化、首页模块增强、悬浮工具栏。
- 已读记录、稍后看、本地标题/作者屏蔽、列表筛选和悬停预览。
- 快捷回复和无刷新提交。
- 广告与干扰项隐藏。
